# Render verification

The shared completion pass now lives in [design-eval](../../design-eval/SKILL.md). Its [render verification mechanics](../../design-eval/references/render-verification.md) cover web geometry, script checks, reloads, and SVG delivery. Use that pass once within the existing review budget.
