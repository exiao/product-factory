interface:
  display_name: "Image Generation Guide"
  short_description: "Vendor-agnostic image briefs, edits, and visual QA"
  default_prompt: "Use $image-generation-guide to create or edit the requested image and save the verified result locally."
policy:
  allow_implicit_invocation: true
